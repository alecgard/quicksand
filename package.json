{
  "name": "quicksand",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build:sandbox": "docker pull ghcr.io/alecgard/quicksand/sandbox:latest && docker tag ghcr.io/alecgard/quicksand/sandbox:latest quicksand-sandbox:latest || docker build -t quicksand-sandbox:latest configs/sandbox/",
    "dev": "pnpm build:sandbox && tsx apps/cli/src/index.ts proxy",
    "test": "node --import tsx --test test/adversarial/*.test.ts test/unit/*.test.ts test/integration/*.test.ts"
  },
  "devDependencies": {
    "@quicksand/manifest": "workspace:*",
    "@quicksand/proxy": "workspace:*",
    "@quicksand/sandbox": "workspace:*",
    "tsx": "^4"
  }
}
